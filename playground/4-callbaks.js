setTimeout(() => {
  console.log('Two seconds are up');
}, 2000);

const names = ['Enzo', 'Jen', 'Jes'];
const shortNames = names.filter(name => {
  return name.length <= 3;
});

const geocode = (address, callback) => {
  const data = {
    latitude: 0,
    longitude: 0
  };

  return data
};

geocode('Philadelfia', )
